# --- core ingest ---
httpx[http2]==0.27.2
curl-cffi==0.7.4          # TLS-fingerprint fallback for 403-ing endpoints
tenacity==9.0.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1
PyYAML==6.0.2
selectolax==0.3.27        # fast HTML -> text (HN comments, descriptions)

# --- storage ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3    # Neon Postgres (SQLite needs nothing)

# --- cli output ---
rich==13.9.4

# --- api ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
